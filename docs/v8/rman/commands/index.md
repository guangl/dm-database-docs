---
title: 命令参考
overview: true
---

dmrman 启动后在 `RMAN>` 提示符下输入命令即可执行备份还原相关操作，也可以通过 `CTLFILE` 或 `CTLSTMT` 启动参数批量执行（参见[使用 dmrman](../usage)）。

以下为 dmrman 支持的主要命令及其说明，按数据备份、备份管理、数据库还原恢复、表空间还原恢复、归档还原修复、操作日志几类排列。
