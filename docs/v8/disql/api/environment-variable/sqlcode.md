---
title: SQLCODE
---

控制台是否打印 `SQLCODE` 返回值。

> [!info]
> `SQLCODE` 返回 0 表示当前 SQL 语句执行成功；
> `SQLCODE` 返回 1 表示当前 SQL 语句执行失败。

## 语法

![语法图](./railroad/sqlcode.svg)

## 参数

* `ON` - 打印返回值；
* `OFF` - 不打印返回值；
