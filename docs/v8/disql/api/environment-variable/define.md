---
title: DEFINE
---

是否使用 DEFINE 定义本地变量。

## 语法

![语法图](./railroad/define.svg)

## 参数

* `c` - 表示打开 DEFINE 功能，同时定义前缀变量符号，`c` 为定义的前缀符号；
* `ON` - 表示打开 DEFINE 功能，使用默认前缀符号 `&`；
* `OFF` - 表示不使用 DEFINE 功能；
