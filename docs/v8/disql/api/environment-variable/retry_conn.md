---
title: RETRY_CONN
---

设置 `disql` 尝试自动重连的次数。

> [!note]
> 该环境变量主要用于写自动化脚本，当连接失败导致 SQL 语句执行失败时，`disql` 尝试自动重新连接数据库，如果在设置次数内连接成功，则重新执行相应 SQL 语句。
> 需要注意的是，该环境变量与 `AUTORECONN` 并无关系。

## 语法

## 参数

* `3` - 默认重连次数为 3；
* `0` - 不自动重连；
* `n` - 自动重连的次数；
