---
title: LOCAL_CODE
---

设置本地编码方式，指定客户端使用的编码格式，会影响结果集和错误信息的编码。

> [!info]
> 设置该参数时会先检查当前是否连接了数据库，若未连接数据库则设置无效，重连数据库后恢复设置为指定的本地编码方式。

## 语法

## 参数

* `GBK`
* `GB18030`
* `UTF8`
* `DEFAULT` - 操作系统的编码方式。
