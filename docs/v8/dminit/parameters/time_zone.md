---
title: TIME_ZONE
---

初始化时区，默认为东八区(+08:00)。格式为[正负号]小时[：分钟]，其中，正负号和分钟为可选项。时区设置范围为：-12:59~+14:00。可选参数。

此参数在数据库创建成功后无法修改，可通过查询 `V$PARAMETER` 或 `V$DM_INI` 中的 `TIME_ZONE` 参数名查看此参数的设置值，还可以通过使用系统函数获取此参数的设置值：`select SF_GET_PARA_VALUE(2,'TIME_ZONE');`。
