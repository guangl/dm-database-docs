---
title: "达梦数据库是否需要设置 hugepage"
---

**【问题描述】**

达梦数据库是否需要设置 hugepage。

**【问题原因】**

hugepage（大页内存）是否需要开启与服务器内存规模及数据库 `buffer` 配置有关，内存规模不同，是否启用 hugepage 的收益也不同，因此需要结合实际情况评估。

**【问题解决】**

512 GB 及以下内存，建议关闭 hugepage。

假若有 1TB 的内存，可以考虑开启 hugepage，此时配置 hugepage 的大小应该略大于 `dm.ini` 中 buffer 的量，比如 120%。
