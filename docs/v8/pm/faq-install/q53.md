---
title: "注册 DM 服务提示 -i 参数无效"
---

**【问题描述】**

注册 DM 服务提示 -i 参数无效。

**【问题分析】**

`-i` 参数是 DM7 的注册服务参数，如果用的是 DM8 数据库，则用 -dm_ini 参数。

例如：注册服务名为 `DmServiceDMSERVER` 的实例服务：
```
./dm_service_installer.sh -t dmserver -dm_ini /dm8/data/DAMENG/dm.ini -p DMSERVER
```
