---
title: "安装数据库报错：Os_file_create_normal error! Desc:permission denied,code:13"
---

**【问题描述】**

安装数据库报错：`os_file_create_normal error! Desc:permission denied,code:13`。

**【问题原因】**

该报错一般是因为对应目录 `dmdba` 没有读写权限，需要用 `root` 用户来操作。

**【问题解决】**

建议利用 `chown` 命令授予安装目录 `dmdba` 权限，不使用 `root` 权限安装数据库。
