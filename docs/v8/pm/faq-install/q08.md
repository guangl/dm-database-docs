---
title: "Openation not Permitted 错误如何解决？"
---

**【问题描述】**

CentOS7 的 Docker 容器里面以静默方式安装达梦数据库最后一步出现报错：`Openation not Permitted`。

**【问题原因】**

Docker 容器默认运行在受限权限模式下，安装达梦数据库过程中部分操作（如注册服务、修改系统配置等）需要更高的系统权限，容器权限不足会导致该步骤执行失败并报错。

**【问题解决】**

在 Docker 中添加 `privileged` 启动参数即可解决。
