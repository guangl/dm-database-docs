---
title: "dminit 初始化数据库时报错：start parameter error"
---

**【问题描述】**

`dminit` 初始化数据库时报错：`start parameter error`。

**【问题原因】**

该报错通常是由于 `dminit` 命令的参数书写不符合语法规则导致，例如参数等号两边存在多余空格，使命令行工具无法正确解析参数。

**【问题解决】**

- 请参考《DM8_dminit 使用手册》语法规则，手册位于数据库安装目录下的 doc 目录中。

- `dminit` 的参数中，等号两边不能有空格。
