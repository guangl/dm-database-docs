---
title: "GLIBC_xxxx not found"
---

**【问题描述】**

数据库安装或者在实例初始化时报错：`version 'GLIBC_xxxx' not found`。

**【问题分析】**

该问题原因为数据库安装包 `glibc` 版本与服务器 `glibc` 版本不匹配问题导致。

**【问题解决】**

推荐以下两种方式解决 ：

1. 重新申请与服务器 `glibc` 版本对应的数据库安装包；

2. 服务器更换与数据库 `glibc` 版本对应的 `glibc。`
