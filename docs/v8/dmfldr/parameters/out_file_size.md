---
title: OUT_FILE_SIZE
---

在导出模式下，设置导出文件大小的最大值，整形数值，有效取值范围为 0~4194303，单位 KB；缺省为 0，表示不限制。设置该参数值非 0 后，导出文件大小将不会超出该值，并且导出文件保证导出完整行数据。当与 `OUT_FILE_ROWS` 参数共同使用时，导出文件时将同时满足这两个参数的限制。此参数为可选参数，仅在导出模式下生效。不能与 `SINGLE_FILE`=`TRUE` 同时使用。
