---
title: PLOG（dmldrc）
---

用于指定 dmldrp 端生成的日志文件及其路径，字符串类型，缺省为 `ldrp.log`（生成在达梦数据库执行码目录同级的 `log` 目录中）。为可选参数。

dmldrc 中的 `LOG` 参数为 dmldrc 端的日志配置路径，`PLOG` 为 dmldrp 端的日志配置路径；`BADFILE` 为 dmldrp 端的错误日志文件配置路径，dmldrc 端不记录错误日志文件。
