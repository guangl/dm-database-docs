---
title: BAD_FILE_MODE
---

错误数据文件 `BADFILE` 的模式。取值 0 或 1，缺省为 0。0：普通模式，错误数据文件中包含文件头、数据头和数据等；1：纯数据模式，错误数据文件中只包含数据内容。

当 `BAD_FILE_MODE`=1 时，默认 bad 文件名为"数据文件名.bad"。对于多表装载，还会为 bad 文件增加编号，编号顺序为控制文件中该数据文件对应表格的顺序。此参数为可选参数。
